const SSOButtons = () => (
  <div>
    <p>Or sign in with:</p>
    <button onClick={() => alert("Google SSO not implemented")}>Google</button>
    <button onClick={() => alert("Microsoft SSO not implemented")}>Microsoft</button>
  </div>
);

export default SSOButtons;
